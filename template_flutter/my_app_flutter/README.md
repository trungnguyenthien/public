
```
flutter create APP_NAME # Create app
flutter doctor ## Check Project Health
flutter devices ## find all connected device
flutter emulators ## List all emulator (Android, iOS)
flutter run -d 'emulator-5556' ## Run on device, use 'devices' to get device name.
flutter test ## Run test code
flutter pub get
flutter pub update
```

Run on Android
```
emulator -list-avds ## List all device
emulator -avd Pixel_5_API_31 -wipe-data ## Run and wipe data, sometime, we need clear data at first
```

CMD
```
# print all env
printenv

# Them env
atom ~/.zshenv
```